# Claude Code Entry Point

This repository uses `AGENTS.md` as the canonical project guide for coding agents.

@AGENTS.md

Claude-specific notes:

- Keep personal or machine-local preferences outside the repository.
- Do not duplicate project rules here; update `AGENTS.md` or the design docs instead.
- Claude-native subagents live in `.claude/agents`, while reusable project skills live in `.agents/skills`.
